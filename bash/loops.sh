#!/usr/bin/bash
# for item in $@; do
#         echo %item
# done
#in this script we are looping through all the user arguments provided to the script.
#$@ is a built-in variable containing all the user arguments.
#we can also define an array of variables in bash and loop through them.